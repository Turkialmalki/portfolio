{
  "// why this exists": "Same pattern as tests/methodology: the knowledge modules are written Deno-style (.ts extension imports) for future Edge Function use, but tests and the seed generator run under plain Node — no Docker, no Deno, no network.",
  "compilerOptions": {
    "target": "es2022",
    "module": "commonjs",
    "moduleResolution": "node10",
    "rewriteRelativeImportExtensions": true,
    "strict": true,
    "types": ["node"],
    "skipLibCheck": true,
    "outDir": "../../../.knowledge-tests-build"
  },
  "include": [
    "harness.ts",
    "../../functions/_shared/knowledge/**/*.ts",
    "../../functions/_shared/methodology/**/*.ts",
    "../../functions/_shared/releaseGates.ts"
  ]
}
