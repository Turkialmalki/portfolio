{
  "// why this exists": "Same pattern as tests/methodology and tests/knowledge: the analysis modules are written Deno-style (.ts extension imports) for the future Edge Function, but tests run under plain Node — no Docker, no Deno, no network, no AI credentials.",
  "compilerOptions": {
    "target": "es2022",
    "module": "commonjs",
    "moduleResolution": "node10",
    "rewriteRelativeImportExtensions": true,
    "strict": true,
    "types": ["node"],
    "skipLibCheck": true,
    "outDir": "../../../.analysis-tests-build"
  },
  "include": [
    "harness.ts",
    "fixtures.ts",
    "../../functions/_shared/analysis/**/*.ts",
    "../../functions/_shared/methodology/**/*.ts",
    "../../functions/_shared/knowledge/**/*.ts",
    "../../functions/_shared/releaseGates.ts"
  ]
}
